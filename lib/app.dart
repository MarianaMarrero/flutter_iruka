import 'package:flutter/material.dart';
import 'package:poc_iruka_flutter/screens/login.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(body: Login());
  }
}
